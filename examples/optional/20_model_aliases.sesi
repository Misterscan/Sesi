/*
    Example 20: Model Aliases

Demonstrates:
     1. set_alias(alias, model)
     2. Using aliases with model()
     3. Using aliases inside workflow() steps
*/

show "=== Registering model aliases ==="
let summary = "summary"
let detailed = "detailed"

summary | set_alias("gemini-3.5-flash-lite")
detailed | set_alias("gemini-3.6-flash")

show "=== Fast summary call ==="
let quick = model(summary) {max_tokens: 100} {"Summarize the benefits of model aliases in two short bullet points."}
show quick

show "=== Deeper reasoning call ==="
let deepAnswer = model(detailed) {thinkingLevel: "medium", max_tokens: 500} {"Give one practical tradeoff between a fast model and a deep reasoning model."}
show deepAnswer

show "=== Workflow with aliased model names ==="
let steps = [
 {"model": summary, "prompt": "Draft a one-line launch update about aliases: "},
 {"model": summary, "prompt": "Tighten wording and remove fluff: "}
 ]

let flow = workflow(steps, "Custom model naming is now available")
show flow["final"]